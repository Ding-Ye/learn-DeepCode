# Quality Report — learn-DeepCode

> Generated by learn-repo-generator skill at the end of Phase H.
> Repo: https://github.com/Ding-Ye/learn-DeepCode
> Upstream snapshot: HKUDS/DeepCode @ b9ece6035ea3f3582e6c503c517206b23c09ad09 (MIT)

## Headline numbers

| Metric | Value |
|---|---|
| Code chapters | 10 / 10 ✅ |
| Integration chapter (s_full) | 1 ✅ |
| Appendices | 2 ✅ (A, B) |
| Multi-model addendum | ✅ |
| Total commits | 8 |
| Sessions passing tests | 10 / 10 ✅ |
| Bilingual zh/en parity (## headings) | 14 / 14 ✅ |
| Six-section completeness (sNN chapters) | 60 / 60 ✅ (10 × 6) |
| Cross-session imports | 0 ✅ (project rule honored) |
| Upstream citations verified | 19 / 19 ✅ (every cited line range exists in local clone) |
| Upstream readings | 10 / 10 ✅ |

## CI status (2026-05-10)

| Workflow | Result |
|---|---|
| `go.yml` | ✅ all runs green (10-session matrix vet+build+test) |
| `docs.yml` | ✅ all runs green (markdown lint + parity) |
| `web.yml` | ⚠️ historic runs failed because `web/package-lock.json` is intentionally not committed. Workflow updated to use `npm install` (instead of `npm ci`) — the next push runs the corrected job. |

## Per-chapter snapshot

| # | Slug | Tests | LOC (Go, non-test) | Doc lines (zh+en avg) |
|---|---|---|---|---|
| s01 | minimum-loop | 5 PASS | ~150 | ~250 |
| s02 | tool-registry | 5 PASS | ~250 | ~270 |
| s03 | config-loader | 5 PASS | ~300 | ~280 |
| s04 | provider-abstraction | 5 PASS | ~400 | ~290 |
| s05 | workflow-context | 5 PASS | ~250 | ~230 |
| s06 | tool-capable-runner | 5 PASS | ~600 | ~280 |
| s07 | planning-runtime | 5 PASS | ~400 | ~260 |
| s08 | loop-detector | 5 PASS | ~300 | ~250 |
| s09 | memory-compaction | 5 PASS | ~500 | ~270 |
| s10 | code-impl-workflow | 5 PASS | ~945 | ~290 |

Total Go non-test LOC across the 10 sessions: ~4,095 (above the plan's 3,950 estimate by ~150 due to s10 being slightly larger than projected — the cost of the strict session-isolation rule, which forces s10 to redeclare the minimal subset of types from s06/s08/s09 instead of importing them).

## Issues by severity

### P0 (blocking) — 0 issues

(none)

### P1 (should-fix) — 1 issue

- **`web` CI was historically failing** because the workflow ran `npm ci` against a missing `web/package-lock.json`. Fixed by switching to `npm install --no-audit --no-fund` in this commit. The next push will exercise the corrected workflow; expected to pass.

### P2 (nice-to-have) — 3 items

- **No Gemini-native provider** in s04. We only ship `AnthropicProvider` and `OpenAIProvider` (the latter handles 7 OpenAI-compatible backends incl. Gemini's compat endpoint). Native Gemini SDK integration is listed as Appendix B exercise #1.
- **MCP stdio framing is simulated** in s02 via `MCPSubprocessTool` (an `io.Closer` placeholder). Real `os/exec` + JSON-RPC framing is Appendix B exercise #5.
- **`workflows/agent_orchestration_engine.py` (~2,312 lines)** is intentionally NOT a chapter. s10 is the architectural apex and Appendix B points readers there if they want the full top-level conductor.

## Anti-patterns deliberately not repeated from upstream

Confirmed avoided in learn-DeepCode (per research-notes.md anti-pattern list):

1. ✅ No global state / module-level singletons (`get_runtime()` style) — all dependencies are constructor-injected
2. ✅ No mixed async/sync — `context.Context` everywhere
3. ✅ No `sys.path.insert` / monkey-patching — proper Go modules
4. ✅ No string/path mixing — `path/filepath` everywhere
5. ✅ No stringly-typed config — `WorkflowContext` (s05) and `Config` (s03) are typed structs
6. ✅ No callback overload — explicit hooks where needed (s10's `OnToolResult`)
7. ✅ No phase-specific logic scattered — each chapter has a single clear entry point
8. ✅ Truncation policy explicit and configurable (`MaxToolBytes` parameter in s06)
9. ✅ Token budgeting explicit via `Tokenizer` interface (s09) — not a hidden tiktoken dep
10. ✅ Loop-detector thresholds configurable (s08 functional-options pattern)

## Reading-flow validation

- Hand-walked through `s01 → s02 → s06 → s_full` and `s01 → s04 → multi-model` paths from the README — both flows make pedagogical sense and don't require out-of-order knowledge.
- Cross-references between docs verified by Read of relative paths.

## Recommendations

- Run `cd web && npm install && npm run typecheck && npm run build` locally to confirm web shell works before publishing.
- For readers learning the most: progress through s01..s10 sequentially over ~3 weeks, doing one of Appendix B's 5 extension exercises after each thematic group.
- After s10, attempt to extend the workflow with a real OpenAI tool-execution backend (Phase G shows the wiring).
